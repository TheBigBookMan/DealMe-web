// TODO clicking on a vnue in the Venue component can send through the venueInfo id if thats best or the google places placeId or the coordinates, whichever will put the maps location to the right spt, will need to adjust in the onClick in venuebio
const Map = () => {
    return (
        <div>
            <p>Map</p>
        </div>
    );
};

export default Map;
